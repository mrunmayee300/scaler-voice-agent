# Minimal deps to RUN the app (chat, ingestion, API) — installs in ~2-5 min
# Use Python 3.11 or 3.12 (NOT 3.14)

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18
sse-starlette==2.1.3

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# LLM & embeddings
openai==1.57.2
tiktoken==0.8.0

# Vector DB
qdrant-client==1.12.1

# Retrieval (reranker skipped — uses score fallback without sentence-transformers)
rank-bm25==0.2.2
numpy==1.26.4

# Timezones (required on Windows for ZoneInfo)
tzdata

# Google Calendar
google-api-python-client==2.154.0
google-auth==2.37.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Database
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0

# HTTP
httpx==0.28.1

# Security
slowapi==0.1.9

# Observability
structlog==24.4.0

# Ingestion
pypdf==5.1.0
langchain-text-splitters==0.3.2
